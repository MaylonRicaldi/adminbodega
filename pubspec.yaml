name: la_bodeguita_admin
description: "Panel administrativo web para La Bodeguita"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

  # 🔥 Firebase - Versiones compatibles
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.0
  http: ^1.1.0
  intl: ^0.18.1
  file_picker: ^8.0.0
  async: ^2.10.0 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true